
// --- meetoo.it: appended by deploy/setup.sh ---------------------------------
// Meetoo answers at the root of its own domain: no /meetoo prefix. The same
// code on isotype.org keeps the default of ws-core/mounts.php, /meetoo.
define('WS_MOUNTS', array());
// --- end meetoo.it ----------------------------------------------------------
